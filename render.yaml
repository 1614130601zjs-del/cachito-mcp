services:
  - type: web
    name: cachito-mcp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: CACHITO_ACCOUNT
        value: 52575934
