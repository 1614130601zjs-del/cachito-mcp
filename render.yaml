services:
  - type: web
    name: cachito-mcp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT app:app
    envVars:
      - key: CACHITO_ACCOUNT
        value: "52575934"
